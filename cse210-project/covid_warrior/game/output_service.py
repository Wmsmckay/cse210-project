import arcade
class Output_service:
    """Outputs the game state. The responsibility of the class of objects is to draw the game state on the terminal. 
    
    Stereotype: 
        Service Provider
        

    Authors:
        Christine Helfrich

    Attributes:
        [insert attributes]
    """
    def __init__(self):
        """ Class constructor. 
        """
        

    def draw_actors(self, actors):
        # for  key, value in actors.items():
        #     # Draw a blue circle
        #     if key == "enemy":
        #         arcade.draw_circle_filled(key.get_position.get_x, key.get_position.get_x, 10, arcade.color.BLUE)       
        #     if key == "player":
        #         arcade.draw_circle_filled(key.get_position.get_x, key.get_position.get_x, 10, arcade.color.GREEN)       
        # for actor in actors:
        #     arcade.
        #     # if actor.get_sprite() == 'player':
        #     #     arcade.draw_circle_filled(actor.get_position().get_x(), actor.get_position().get_y(), 10, arcade.color.GREEN)
        #     # else: 
        #     #     arcade.draw_circle_filled(actor.get_position().get_x(), actor.get_position().get_y(), 10, arcade.color.BLUE)       
        pass